; AquaChord API — ค่า PHP ต่อโฟลเดอร์ (PHP-FPM อ่าน .user.ini เอง, แคช user_ini.cache_ttl = 300 วิ)
; อัปโหลดไฟล์เสียงให้ GPU (docs/09 §3): เพดานจริงฝั่งโค้ด = min(64 MB, เพดาน aixman จาก ping)
; post_max_size ต้องมากกว่า upload_max_filesize เผื่อ field อื่นใน multipart
upload_max_filesize = 64M
post_max_size = 70M
max_file_uploads = 2
; ส่งต่อไป aixman ใช้ได้ถึง 120 วิ (+ สร้างงาน) — Linux นับเฉพาะเวลา CPU แต่ตั้งกันไว้
max_execution_time = 180
max_input_time = 180
